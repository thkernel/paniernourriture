<%= form_with(model: food_restaurant) do |form| %>
  <% if food_restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food_restaurant.errors.count, "error") %> prohibited this food_restaurant from being saved:</h2>

      <ul>
        <% food_restaurant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :uid %>
    <%= form.text_field :uid %>
  </div>

  <div class="field">
    <%= form.label :food_id %>
    <%= form.text_field :food_id %>
  </div>

  <div class="field">
    <%= form.label :restaurant_id %>
    <%= form.text_field :restaurant_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
