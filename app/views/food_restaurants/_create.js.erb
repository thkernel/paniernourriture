$("ul.errors").html("");
<% if @food_restaurant.errors.any? %>
  <% @food_restaurant.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".food-categories").html("<%= escape_javascript(render 'index') %>");
  $("#food-restaurant-modal").modal("hide");
 
<% end %>